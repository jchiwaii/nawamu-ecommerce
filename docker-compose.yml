services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: shoe_store
      POSTGRES_USER: shoe_store
      POSTGRES_PASSWORD: shoe_store
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
